package Codes;

public class Args {
    /*
     * === 1 === If we pass int[] arguments insted of String[] argumentster it will give runtime error 
     * ``` error: can't find main(String[]) method in class: Codes.Args ```
     * 
     * === 2 === If we can't delclare main method as public static it will give compilation error 
     * error: 'main' method is not declared 'public static'
     */
    public static void main(String [] arguments) {
        System.out.println("YOYO");
    }
}
